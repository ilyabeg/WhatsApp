<UserControl x:Class="WhatsAppUI.View.UserControls.ActiveUsersList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WhatsAppUI.View.UserControls"
             
             xmlns:models="clr-namespace:Client.Interfaces;assembly=Client"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="180">

    <Grid Background="{StaticResource UsersListBrush}" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="0" Content="{Binding ThisClient}" />

        <ListBox Grid.Row="1" Background="Transparent" 
                 ItemsSource="{Binding ChatItems}" 
                 SelectedItem="{Binding SelectedChat, Mode=TwoWay}">
            
            <!-- No data template because WPF automatically pulls template 
                 from the Material Design resource dict -->
            
        </ListBox>
        
    </Grid>

</UserControl>
