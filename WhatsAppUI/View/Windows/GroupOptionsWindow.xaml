<Window x:Class="WhatsAppUI.View.Windows.GroupOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WhatsAppUI.View.Windows"
        mc:Ignorable="d"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Title="Group Options" Height="250" Width="400">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtBlock" Grid.Row="0" Text="Enter Group Name:" FontSize="16" FontWeight="Medium" Margin="0,0,0,10"/>

        <!-- Create Group textbox -->
        <TextBox x:Name="InputTextBox" Grid.Row="1" VerticalAlignment="Top" FontSize="16" />

        <!-- Join or Leave listbox of Groups -->
        <ListBox x:Name="GroupListBox" Grid.Row="1" Visibility="Collapsed" FontSize="16">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" IsCancel="True" Margin="0,0,10,0" Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Content="OK" IsDefault="True" Click="OkButton_Click" />
        </StackPanel>
    </Grid>

</Window>
